---
- name: Install ansible
  hosts: all
  gather_facts: no
  roles:
    - setup_server
    - role: setup_ssh_config
      vars:
        server_name: "ansible"
        env_var: "ANSIBLE_IP"

- name: Setup ansible
  hosts: all
  gather_facts: no
  become: true
  become_user: "{{ user_name }}"
  roles:
    - setup_ansible
